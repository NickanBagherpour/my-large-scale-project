'use client';

import <%= className %> from '@oxygen/<%= shortPortalName %>/widgets/<%= fileName %>';

export default function Index(props) {
   return (
     <<%= className %> parentProps={props} />
   );
}
