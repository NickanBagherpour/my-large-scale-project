'use client';

import ScopeManagementWidget from '@oxygen/backoffice/widgets/scope-management-widget';

export default function Index(props) {
  return <ScopeManagementWidget parentProps={props} />;
}
