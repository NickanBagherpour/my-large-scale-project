'use client';

import RequestRegistrationWidget from '@oxygen/customer/widgets/request-registration-widget';

export default function Index(props) {
  return <RequestRegistrationWidget parentProps={props} />;
}
