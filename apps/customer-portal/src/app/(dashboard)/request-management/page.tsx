'use client';

import RequestManagementWidget from '@oxygen/customer/widgets/request-management-widget';

export default function Index(props) {
  return <RequestManagementWidget parentProps={props} />;
}
