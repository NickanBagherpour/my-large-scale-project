'use client';

import RequestDetailsInfoWidget from '@oxygen/customer/widgets/request-details-info-widget';

export default function Index(props) {
  return <RequestDetailsInfoWidget parentProps={props} />;
}
