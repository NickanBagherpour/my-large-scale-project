'use client';

import { LandingLayout } from '@oxygen/customer/layouts';

export default function Layout({ children }: { children: React.ReactNode }) {
  return <LandingLayout>{children}</LandingLayout>;
}
