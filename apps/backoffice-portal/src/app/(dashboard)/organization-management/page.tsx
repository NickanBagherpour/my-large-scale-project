'use client';

import OrganizationManagementWidget from '@oxygen/backoffice/widgets/organization-management-widget';

export default function Index(props) {
  return <OrganizationManagementWidget parentProps={props} />;
}
