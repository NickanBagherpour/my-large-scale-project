import { FetchParamsType } from '../types';

const Api = {
  // getServiceHistoryData: async (params: FetchParamsType) => {
  //   return client.post<ServiceHistoryResponseType>(`${portalUrl}/v1/redemption/report`, params);
  // },
  // getServiceHistoryData: async ({id,...rest}: FetchParamsType) => {
  //   return client.get<ServiceHistoryResponseType>(`${portalUrl}/v1/services/${id}/history`, {params: rest });
  // },
};
export default Api;
