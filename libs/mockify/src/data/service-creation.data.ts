import type { Scope } from '@oxygen/types';

export const items: Scope[] = Array.from({ length: 20 }).map((_, idx) => ({
  idx,
  scopeName: 'svc-gfg-bhhj-ngdc-zxzxc-zxc',
  persianName: 'دریافت کد‌های ملی متعلق به یک شماره موبایل',
}));
